# PowerShell 脚本使用说明

所有 PowerShell 脚本的编码问题已修复，所有中文字符已替换为英文，避免乱码。

## ✅ 已修复的脚本

1. **`lan_start_compare.ps1`** - 启动服务（已内嵌评审员链接显示）
2. **`lan_stop_compare.ps1`** - 停止服务
3. **`scripts/setup_firewall_compare.ps1`** - 配置防火墙
4. **`export_all_compare.ps1`** - 导出数据

## 🚀 使用方法

### 1. 启动服务（推荐使用）

```powershell
# 在项目根目录执行
.\lan_start_compare.ps1
```

**功能**：
- ✅ 启动视频服务器（端口 8011）
- ✅ 启动 Streamlit UI（端口 8503）
- ✅ 启动监控服务
- ✅ **自动显示所有评审员的访问链接**（无需再运行 get_links_compare.py）

**输出示例**：
```
============================================================
 Compare Mode - Starting Services
============================================================

Local IP: 172.16.82.12

[1/3] Starting video server (port 8011)...
  Video server started (Job ID: 1)

[2/3] Starting Streamlit UI (port 8503)...
  Streamlit UI started (Job ID: 2)

[3/3] Starting monitor service (every 300 seconds)...
  Monitor service started (Job ID: 3)

============================================================
 Services Started Successfully!
============================================================

============================================================
 Judge Access Links
============================================================

Local IP: 172.16.82.12
UI Port: 8503

[Judge-01]
  Local:   http://localhost:8503/?uid=abc123...
  Network: http://172.16.82.12:8503/?uid=abc123...

[Judge-02]
  Local:   http://localhost:8503/?uid=def456...
  Network: http://172.16.82.12:8503/?uid=def456...

...

============================================================

To stop services: .\lan_stop_compare.ps1

Press any key to exit (services will keep running)...
```

### 2. 停止服务

```powershell
.\lan_stop_compare.ps1
```

### 3. 配置防火墙

```powershell
# 以管理员身份运行 PowerShell，然后执行：
.\scripts\setup_firewall_compare.ps1
```

### 4. 导出数据

```powershell
.\export_all_compare.ps1
```

## 💡 重要说明

### 关于编码问题

所有脚本已将中文字符替换为英文，确保在任何 Windows 系统上都不会出现编码错误。

### 内嵌的评审员链接功能

`lan_start_compare.ps1` 现在会在启动服务后**自动显示所有评审员的访问链接**，包括：
- 本机访问地址（localhost）
- 局域网访问地址（实际IP）

无需再单独运行 `python get_links_compare.py`。

### 如果仍有问题

如果启动时遇到执行策略错误，请先运行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

## 📋 完整的首次部署流程

```powershell
# 1. 准备数据
python scripts\prepare_data_compare.py

# 2. 初始化数据库
python scripts\setup_project_compare.py --judges 10

# 3. 配置防火墙（可选，需要管理员权限）
# 以管理员身份运行 PowerShell，然后：
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\scripts\setup_firewall_compare.ps1

# 4. 启动服务（会自动显示评审员链接）
.\lan_start_compare.ps1
```

启动后，脚本会自动显示所有评审员的访问链接，直接分发给评审员即可！

## 🎯 关键改进

1. **编码修复**：所有中文字符替换为英文，避免乱码
2. **内嵌链接**：启动脚本自动显示评审员链接，无需额外命令
3. **统一风格**：所有脚本使用英文输出，保持一致性
4. **简化流程**：一个命令完成启动和链接显示

---

**现在可以直接使用 `.\lan_start_compare.ps1` 启动系统！**

